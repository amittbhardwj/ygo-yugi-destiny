services:
  - type: web
    name: ygo-yugi-destiny
    env: node
    region: oregon
    buildCommand: rm -rf client/dist && npm install && npm run build
    startCommand: npm start
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001